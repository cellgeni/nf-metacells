# This file was auto-generated by `nextflow module spec`.
#
# Review and complete all fields marked "TODO" before publishing.
#

$schema: https://raw.githubusercontent.com/nextflow-io/schemas/refs/heads/main/module/v1/schema.json
name: cellgeni/seacells
version: 1.0.0
description: SEACells aggregates single-cell profiles into metacells (cellular states)
  from scRNA-seq or scATAC-seq data.
license: MIT
input:
- - name: meta
    type: map
    description: Sample metadata map (must include an `id` key used for tagging/prefixing
      outputs).
  - name: adata
    type: file
    description: Input AnnData object (`.h5ad`) containing a single sample and the
      required matrix/embeddings.
output:
- - name: meta
    type: map
    description: The same metadata map received as input.
  - type: file
    description: Output AnnData metacell object(s) (`*.h5ad`; typically includes `seacell_metacells.h5ad`).
- - name: meta
    type: map
    description: The same metadata map received as input.
  - type: file
    description: CSV result tables (`*.csv`; includes hard/soft metacell assignments).
- - name: meta
    type: map
    description: The same metadata map received as input.
  - type: file
    description: Numpy arrays (`*.npy`; includes SEACells assignment weights).
- - name: meta
    type: map
    description: The same metadata map received as input.
  - type: file
    description: PDF diagnostics (`*.pdf`; convergence, assignment, and metacell QC
      plots).
- - name: meta
    type: map
    description: The same metadata map received as input.
  - type: file
    description: Pickled SEACells model object(s) (`*.pkl`).
- name: versions
  type: file
  description: YAML file containing software versions used in the module execution.
